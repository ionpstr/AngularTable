import { TransformTypePipe } from './transform-type.pipe';

describe('TransformTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TransformTypePipe();
    expect(pipe).toBeTruthy();
  });
});
